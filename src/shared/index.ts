/**
 * Central barrel export for all shared resources
 *
 * Exports both types and UI components from a single entry point
 *
 * Usage:
 * - Types: import type { Voucher } from '@/shared'
 * - Components: import { Button } from '@/shared'
 */

// Re-export all types
export * from './types';

// Re-export all UI components
export * from './ui';

// Re-export Constants
export * from './constants';
